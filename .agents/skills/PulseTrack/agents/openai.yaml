interface:
  display_name: "PulseTrack"
  short_description: "Repo-specific patterns and workflows for PulseTrack"
  default_prompt: "Use the PulseTrack repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true