changes:
  export-excel-sanitize-parent:
    title: Backlog Excel export — sanitize description HTML + add Parent Task Key column
    tasks_md: archive/export-excel-sanitize-parent/tasks.md
    depends_on: []
    skills: []
    status: done
    owner: "worker-cc"
    archived: archive/export-excel-sanitize-parent
    notes: "vitest export-excel.spec.ts passes (3 scenarios); pnpm build clean"
  enhance-project-card:
    title: Project card redesign — logo, member avatars, pinned footer
    tasks_md: changes/enhance-project-card/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: doing
    owner: "worker-1"
    notes: ""
  redesign-dashboard:
    title: Dashboard redesign — status strip, bug banner, layout restyle per mockup
    tasks_md: archive/redesign-dashboard/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: done
    owner: "worker-2"
    archived: archive/redesign-dashboard
    notes: ""
  add-agent-write-consent:
    title: MCP agent write consent — per-token opt-in gate on write tools
    tasks_md: archive/add-agent-write-consent/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: done
    owner: "worker-cc"
    archived: archive/add-agent-write-consent
    notes: "verifies narrowed to this change's tests; 4 pre-existing unrelated test failures (3 mcp-tools.spec, 1 McpAccessCard revoked-badge) ruled out of scope"
  add-sprint-edit:
    title: Edit sprint name and dates — dual-mode CreateSprintDialog, two-week default
    tasks_md: archive/add-sprint-edit/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: done
    owner: "worker-cc"
    archived: archive/add-sprint-edit
    notes: "verified live (anle@CARIS): all 4 reqs pass; no HTML mockup so style-diff rung skipped, reused shadcn Button/Dialog"
  compact-kanban-card:
    title: Compact three-zone Kanban task card + responsive column widths
    tasks_md: archive/compact-kanban-card/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: done
    owner: "worker-cc"
    archived: archive/compact-kanban-card
    notes: "verified live (anle@CARIS backlog board): 3-zone compact card + responsive columns pass; req-2 overdue border code+build verified (no overdue task on board to render live)"
  add-realtime-chat:
    title: Real-time chat backend — DM + standalone channels, ChatGateway, attachments, typing/presence/read/unread/edit/soft-delete/history
    tasks_md: archive/add-realtime-chat/tasks.md
    depends_on: []
    skills: []
    status: done
    owner: "worker-cc"
    archived: archive/add-realtime-chat
    notes: "All 9 sections pass (65 API tests green, build clean). Migration generated via schema-to-schema diff to avoid a PRE-EXISTING EntityType.MESSAGE drift; verified on a local scratch DB (shared team DB untouched). Added a member-gated chat:join socket handler (room delivery glue not separately spec'd)."
  add-realtime-chat-web:
    title: Real-time chat frontend — chat page (Channels/DMs split, thread, pill composer), /chat socket, inline-image attachments (copy + paste/drag), new-conversation overlay, AppSidebar Chat item + /chat route
    tasks_md: archive/add-realtime-chat-web/tasks.md
    depends_on: [add-realtime-chat]
    skills: [frontend-design]
    status: done
    owner: "worker-cc"
    archived: archive/add-realtime-chat-web
    notes: "Done 2026-09-21. All 8 sections: §1/§2 vitest green (5 tests), §3-8 UI built + 6 chat unit tests green + pnpm build clean; §3-8 visual gate via devspec-verify PASS (live app, anle@; empty/overlay/filled screenshots in verify/). Search reuses project-scoped members/search (people-only) per design Decision Default (narrows req-3 §6 unjoined-channel/cross-project). §8.2 MANUAL BA-approved by user (auto go). Pre-existing unrelated failure McpAccessCard.test (revoked-badge) out of scope. Consumes backend REST + /chat socket. Access via new AppSidebar item + /chat route; unread badge separate from NotificationBell. Reuse planner chat components (ChatMessage/ChatInput/ChatMessageList/ChatAttachment) + shadcn. Approved mockup at mockups/chat-page.html. §8.2 is a MANUAL BA screenshot gate."
  chat-delete-conversation:
    title: Delete a DM for yourself — three-dot header menu + confirm dialog, hard-deletes the caller's own ConversationMember row
    tasks_md: archive/chat-delete-conversation/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: done
    owner: "worker-cc"
    archived: archive/chat-delete-conversation
    notes: "All 3 sections pass. §1 api chat tests green (leaveConversation deletes only caller's ConversationMember row). §2 web useChat tests green (cache drop + active-id clear). §3 devspec-verify live PASS (anle@): DM header ⋯ present, channel header none, styled AlertDialog confirm, confirm removed DM from list + cleared pane. pnpm build clean. No mockup → per-property diff skipped; reused shadcn Button/DropdownMenu/AlertDialog. Verify filter names in tasks.md said @pulsetrack/* but real package is @pm/* (used actual). Pre-existing unrelated McpAccessCard.test failure out of scope."
  global-search:
    title: Global search palette (Cmd+K) across projects + tasks + capped sidebar projects scroll
    tasks_md: changes/global-search/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: doing
    owner: "worker-cc"
    notes: ""
  chat-members-mentions:
    title: Chat channel usability — DM|Channel create toggle + member multiselect, members panel (add any-member / remove owner-only / leave), @mention autocomplete + colored render + real-time toast (Option A, no schema)
    tasks_md: changes/chat-members-mentions/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: blocked
    owner: "worker-cc"
    notes: "§1-§8 all done + verified (API 26 chat.service + gateway tests green; web unit tests green; §5-§8 devspec-verify live PASS @localhost:5173 anle@, screenshots in verify/). BLOCKED on §9.1 MANUAL: BA visual sign-off — box unticked, worker never self-approves. To resume: BA ticks §9.1 [x] + reset status blocked→pending. See report/blockers.md."
  dual-auth:
    title: Dual auth — external customer email/password login alongside Keycloak; invite→set-password, silent refresh, anti-squatting + directory-scoping isolation
    tasks_md: archive/dual-auth/tasks.md
    depends_on: []
    skills: [frontend-design]
    status: done
    owner: "worker-cc"
    archived: archive/dual-auth
    notes: "All 9 sections done + verified. API: 40 tests green (dual strategy, anti-squatting, argon2 login/refresh-rotate, set/forgot-password, throttler 5/60s IP+email, external invite, directory scoping) + nest build clean. Migration hand-authored (additive) + replay-verified on scratch DB (INTERNAL/ACTIVE backfill) — Prisma 7 migration engine (rtk) absent here so `migrate diff/dev` can't run; team DB NOT migrated (human applies). §7: searchTargets was UNSCOPED (chat-members-mentions didn't scope it) so scoped it + users.findAll; search.service has no user results. Web build clean; /devspec-verify PASS live (agent-browser, web:5173 + API on scratch DB:3000) — all 3 auth pages + error/sent/invalid-token states; one fix applied+reverified (AuthShell used prefers-color-scheme media query → switched to app's .dark class strategy). Added deps @node-rs/argon2 + @nestjs/throttler. NEEDS ops before prod: apply migration to team DB + set EXTERNAL_JWT_SECRET (code reads from config; used a temp secret only for local verify). Socket/planner-SSE still read keycloak.token directly (out of §9.3 scope = api.ts) — external chat/planner a follow-up. ui.md BA-screenshot gate is advisory (not encoded as a MANUAL subtask); screenshots in verify/."
