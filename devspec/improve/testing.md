# Testing

## App URL
- Web (Vite dev): http://localhost:5173
- Auth: Keycloak redirect to `auth.cyberlogitec.com.vn` (realm CLV, client `PulseTrackLocal`)

## Test account
- USER ID: `anle`
- Password: `acbd@`

Use this account for `/devspec-verify` browser UI checks (login via the Keycloak "USER ID" / "PASSWORD" fields).
