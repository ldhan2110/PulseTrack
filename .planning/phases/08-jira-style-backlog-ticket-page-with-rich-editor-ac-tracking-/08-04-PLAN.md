---
phase: 08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - apps/web/src/pages/TaskDetailPage.tsx
  - apps/web/src/components/tasks/CommentThread.tsx
  - apps/web/src/components/tasks/CommentItem.tsx
  - apps/web/src/components/tasks/CommentComposer.tsx
  - apps/web/src/components/tasks/AttachmentList.tsx
  - apps/web/src/components/tasks/ActivityLog.tsx
  - apps/web/src/components/tasks/ActivityEntry.tsx
autonomous: false
requirements:
  - COLB-01
  - COLB-02
  - REQ-RTE
  - REQ-AC
  - REQ-ATTACH
  - REQ-HIST

must_haves:
  truths:
    - "TaskDetailPage renders a two-panel layout with left content (description, AC, comments, attachments, activity) and right sidebar (status, assignee, sprint, points, sub-tasks, delete)"
    - "Description uses Tiptap RichTextEditor with auto-save on blur"
    - "AC section shows completion badge (X/Y done) and supports inline editing"
    - "Comment thread shows top-level comments with one-level reply indentation"
    - "User can post a comment and reply to an existing comment"
    - "User can delete own comment (or PM can delete any comment)"
    - "User can upload an attachment (max 10 MB client-side check) and download/delete attachments"
    - "Activity log shows chronological history of field changes with actor name and relative timestamp"
    - "Sidebar is sticky while left panel scrolls"
  artifacts:
    - path: "apps/web/src/pages/TaskDetailPage.tsx"
      provides: "Full two-panel ticket page"
      min_lines: 200
    - path: "apps/web/src/components/tasks/CommentThread.tsx"
      provides: "Comment list with reply forms"
      contains: "CommentThread"
    - path: "apps/web/src/components/tasks/CommentItem.tsx"
      provides: "Single comment with reply/delete"
      contains: "CommentItem"
    - path: "apps/web/src/components/tasks/CommentComposer.tsx"
      provides: "Plain text comment input"
      contains: "CommentComposer"
    - path: "apps/web/src/components/tasks/AttachmentList.tsx"
      provides: "File list with upload/download/delete"
      contains: "AttachmentList"
    - path: "apps/web/src/components/tasks/ActivityLog.tsx"
      provides: "History timeline container"
      contains: "ActivityLog"
    - path: "apps/web/src/components/tasks/ActivityEntry.tsx"
      provides: "Single history entry"
      contains: "ActivityEntry"
  key_links:
    - from: "apps/web/src/pages/TaskDetailPage.tsx"
      to: "RichTextEditor"
      via: "import and render in left panel"
      pattern: "RichTextEditor"
    - from: "apps/web/src/pages/TaskDetailPage.tsx"
      to: "useComments, useAttachments, useTaskHistory"
      via: "hook calls"
      pattern: "useComments|useAttachments|useTaskHistory"
    - from: "apps/web/src/components/tasks/CommentThread.tsx"
      to: "CommentItem, CommentComposer"
      via: "child component composition"
      pattern: "CommentItem|CommentComposer"
---

<objective>
Replace the existing TaskDetailPage with a full two-panel Jira-style ticket page. Build all remaining UI components: CommentThread, CommentItem, CommentComposer, AttachmentList, ActivityLog, ActivityEntry. Wire all hooks and the RichTextEditor into the final page.

Purpose: This is the user-facing deliverable — the complete ticket page with rich description editing, threaded comments, file attachments, and activity history. All prior plans were infrastructure for this plan.
Output: Complete TaskDetailPage replacement with 6 new components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-CONTEXT.md
@.planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-RESEARCH.md
@.planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-UI-SPEC.md
@.planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-02-SUMMARY.md
@.planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-03-SUMMARY.md

<interfaces>
<!-- Components and hooks created in prior plans -->

From Plan 03 - RichTextEditor:
```typescript
export function RichTextEditor({
  initialContent,
  onSave,
  editable,
}: {
  initialContent: string;
  onSave: (html: string) => void;
  editable: boolean;
}) // renders Tiptap with toolbar
```

From Plan 03 - useComments hooks:
```typescript
export function useComments(projectId: string, taskId: string)
export function useCreateComment(projectId: string, taskId: string)
  // mutationFn: (content: string) => ...
export function useCreateReply(projectId: string, taskId: string)
  // mutationFn: ({ commentId, content }) => ...
export function useDeleteComment(projectId: string, taskId: string)
  // mutationFn: (commentId: string) => ...
```

From Plan 03 - useAttachments hooks:
```typescript
export function useAttachments(projectId: string, taskId: string)
export function useUploadAttachment(projectId: string, taskId: string)
  // mutationFn: (file: File) => ...
export function useDeleteAttachment(projectId: string, taskId: string)
  // mutationFn: (attachmentId: string) => ...
```

From Plan 03 - useTaskHistory hook:
```typescript
export function useTaskHistory(projectId: string, taskId: string)
```

From Plan 03 - types:
```typescript
export interface Comment {
  id: string; content: string; taskId: string; authorId: string;
  parentId: string | null; createdAt: string; updatedAt: string;
  author: Pick<User, 'id' | 'username' | 'email'>; replies?: Comment[];
}
export interface Attachment {
  id: string; filename: string; storedName: string; mimeType: string;
  size: number; taskId: string; uploaderId: string; createdAt: string;
  uploader: Pick<User, 'id' | 'username' | 'email'>;
}
export interface TaskHistoryEntry {
  id: string; taskId: string; actorId: string; field: string;
  oldValue: string | null; newValue: string | null; createdAt: string;
  actor: Pick<User, 'id' | 'username' | 'email'>;
}
```

From Plan 03 - api.ts:
```typescript
api.getAttachmentDownloadUrl(projectId, taskId, attachmentId) // returns URL string
```

From existing codebase:
```typescript
// apps/web/src/hooks/useProjectRole.ts
export function useProjectRole(projectId: string): {
  role: ProjectRole | null; canManage: boolean; canEdit: boolean;
}
// canManage = role === 'pm'; canEdit = role !== null

// apps/web/src/pages/TaskDetailPage.tsx — existing helpers to reuse:
function parseAcceptanceCriteria(json: string | null | undefined): AcceptanceCriteria[]
function serializeAcceptanceCriteria(items: AcceptanceCriteria[]): string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comment, Attachment, and ActivityLog components</name>
  <files>
    apps/web/src/components/tasks/CommentThread.tsx,
    apps/web/src/components/tasks/CommentItem.tsx,
    apps/web/src/components/tasks/CommentComposer.tsx,
    apps/web/src/components/tasks/AttachmentList.tsx,
    apps/web/src/components/tasks/ActivityLog.tsx,
    apps/web/src/components/tasks/ActivityEntry.tsx
  </files>
  <read_first>
    apps/web/src/pages/TaskDetailPage.tsx,
    apps/web/src/components/tasks/RichTextEditor.tsx,
    apps/web/src/hooks/useComments.ts,
    apps/web/src/hooks/useAttachments.ts,
    apps/web/src/hooks/useTaskHistory.ts,
    apps/web/src/lib/types.ts,
    apps/web/src/lib/api.ts,
    apps/web/src/components/tasks/StatusBadge.tsx,
    apps/web/src/components/ui/avatar.tsx,
    apps/web/src/components/ui/alert-dialog.tsx,
    .planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-UI-SPEC.md
  </read_first>
  <action>
Create 6 new components following the UI-SPEC exactly:

**1. `CommentComposer.tsx`** — Plain text input + "Post Comment" button.
Props: `onSubmit: (content: string) => void`, `isPending: boolean`, `placeholder?: string` (default "Add a comment..."), `onCancel?: () => void` (for reply mode).
- Uses `Textarea` (rows=2, resize-none) + `Button` (primary, "Post Comment") + optional `Button` (ghost, "Cancel").
- Clears textarea on successful submit.
- Disable button when textarea is empty or isPending.
- When `onCancel` prop is present, show Cancel button (for inline reply composers).
- Reply placeholder: "Write a reply..." (passed by CommentThread when rendering reply composers).

**2. `CommentItem.tsx`** — Single comment display.
Props: `comment: Comment`, `currentUserId: string`, `canManage: boolean`, `onReply: (commentId: string) => void`, `onDelete: (commentId: string) => void`, `isReply?: boolean`.
- Layout: Avatar (size-6) + author name (text-sm font-medium) + relative timestamp (`formatDistanceToNow(new Date(comment.createdAt), { addSuffix: true })` from date-fns) + body text (text-sm) + Reply button (ghost, text-xs) + Delete button (ghost icon Trash2, visible only if `comment.authorId === currentUserId || canManage`).
- Reply button not shown if `isReply` is true (no nested replies beyond one level per D-06).
- Delete uses AlertDialog: Title "Delete Comment", Body "This will permanently delete this comment. This action cannot be undone.", Confirm "Delete Comment" (destructive), Cancel "Cancel".
- Import `Trash2, Reply` from lucide-react.

**3. `CommentThread.tsx`** — Full comment list + composers.
Props: `projectId: string`, `taskId: string`, `currentUserId: string`, `canManage: boolean`.
- Uses `useComments(projectId, taskId)`, `useCreateComment(projectId, taskId)`, `useCreateReply(projectId, taskId)`, `useDeleteComment(projectId, taskId)`.
- Renders top-level comments via `CommentItem`. Below each top-level comment, renders its `comment.replies` array with `pl-8` indent and `isReply=true`.
- Tracks `replyingTo: string | null` state — when Reply is clicked on a comment, shows an inline `CommentComposer` below that comment's replies.
- Bottom of the list: always show a `CommentComposer` for new top-level comments.
- Empty state: "No comments yet. Start the conversation." (text-sm text-muted-foreground, centered).
- Section header: `<h2 className="text-[13px] font-semibold text-muted-foreground">Comments</h2>`

**4. `AttachmentList.tsx`** — File list with upload and delete.
Props: `projectId: string`, `taskId: string`, `currentUserId: string`, `canManage: boolean`.
- Uses `useAttachments(projectId, taskId)`, `useUploadAttachment(projectId, taskId)`, `useDeleteAttachment(projectId, taskId)`.
- Upload button: `<Button variant="outline" size="sm" className="gap-2"><Upload className="size-3.5" /> Attach file</Button>` triggers hidden `<input type="file" className="sr-only" aria-label="Upload attachment">` via ref.
- **Client-side 10 MB check**: Before calling upload mutation, check `if (file.size > 10_485_760)` and show `toast.error('File is too large. Maximum size is 10 MB.')` and return early.
- Upload in-flight: show `Loader2 className="size-3.5 animate-spin"` on button, disable button.
- Each row: `flex items-center gap-3 py-2 border-b last:border-b-0`. Content: file icon (use `FileText` for text/* mimeType, `ImageIcon` for image/*, `File` for everything else) + filename (text-sm font-medium, truncate max-w-[240px]) + formatted size + uploader avatar (size-5) + uploader name (text-xs text-muted-foreground) + relative date (text-xs) + download link (`<a href={api.getAttachmentDownloadUrl(...)} target="_blank">` with Download icon) + delete button (ghost icon Trash2 size-3.5, visible only if uploader or PM).
- File size formatting: < 1024 = `${n} B`; < 1048576 = `${(n / 1024).toFixed(1)} KB`; else `${(n / 1048576).toFixed(1)} MB`.
- Delete: AlertDialog with Title "Remove Attachment", Body "This will permanently delete {filename}. This action cannot be undone.", Confirm "Remove" (destructive), Cancel "Cancel".
- Empty state: "No attachments." (text-sm text-muted-foreground).
- Section header: `<h2 className="text-[13px] font-semibold text-muted-foreground">Attachments</h2>`

**5. `ActivityEntry.tsx`** — Single history item.
Props: `entry: TaskHistoryEntry`, `members?: Member[]`, `sprints?: Sprint[]`.
- Layout: Avatar (size-6) of actor + actor name (text-xs font-medium) + change description (text-xs text-muted-foreground) + relative timestamp (text-xs text-muted-foreground).
- History entry text templates (from RESEARCH.md):
  - `status`: "moved to {STATUS_LABEL}" where labels are: BACKLOG="Backlog", IN_PROGRESS="In Progress", IN_REVIEW="In Review", DONE="Done", BLOCKED="Blocked"
  - `assigneeId`: if newValue is null/empty "removed assignee", else "assigned to {member username}" (look up from members array by userId)
  - `sprintId`: if newValue is null/empty "removed from sprint", else "moved to sprint {sprint name}" (look up from sprints array)
  - `storyPoints`: if newValue is null/empty "cleared story points", else "set story points to {newValue}"
  - `title`: "renamed to \"{newValue}\""
  - default: "changed {field}"

**6. `ActivityLog.tsx`** — Read-only chronological timeline.
Props: `projectId: string`, `taskId: string`, `members?: Member[]`, `sprints?: Sprint[]`.
- Uses `useTaskHistory(projectId, taskId)`.
- Receives data sorted descending from API, reverses for chronological ascending display (oldest at top per D-22/UI-SPEC).
- Timeline layout: vertical list with a 1px connector line (`border-l border-border ml-3`) connecting entries.
- Empty state: "No activity yet." (text-sm text-muted-foreground).
- Error state: "Could not load activity. Refresh to try again." (text-sm text-muted-foreground).
- Section header: `<h2 className="text-[13px] font-semibold text-muted-foreground">Activity</h2>`

All components use existing shadcn/ui primitives (Button, Avatar, Badge, AlertDialog, Textarea, Tooltip, Separator) already available in the project.
  </action>
  <verify>
    <automated>cd /Users/admin/Desktop/Projects/PM && pnpm --filter @pm/web exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/tasks/CommentThread.tsx exists and contains `useComments`
    - apps/web/src/components/tasks/CommentItem.tsx exists and contains `CommentItem`
    - apps/web/src/components/tasks/CommentItem.tsx contains `AlertDialog` (delete confirmation)
    - apps/web/src/components/tasks/CommentItem.tsx contains `formatDistanceToNow`
    - apps/web/src/components/tasks/CommentComposer.tsx exists and contains `Post Comment`
    - apps/web/src/components/tasks/AttachmentList.tsx exists and contains `useUploadAttachment`
    - apps/web/src/components/tasks/AttachmentList.tsx contains `10_485_760` (client-side 10 MB check)
    - apps/web/src/components/tasks/AttachmentList.tsx contains `getAttachmentDownloadUrl`
    - apps/web/src/components/tasks/AttachmentList.tsx contains `AlertDialog` (delete confirmation)
    - apps/web/src/components/tasks/ActivityLog.tsx exists and contains `useTaskHistory`
    - apps/web/src/components/tasks/ActivityLog.tsx contains `.reverse()` or `.slice().reverse()` or `[...data].reverse()`
    - apps/web/src/components/tasks/ActivityEntry.tsx exists and contains `moved to`
    - apps/web/src/components/tasks/ActivityEntry.tsx contains `assigned to`
    - `pnpm --filter @pm/web exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 6 UI components built: CommentThread (threaded display + reply management), CommentItem (single comment with delete), CommentComposer (plain text input), AttachmentList (upload/download/delete with 10 MB check), ActivityLog (reversed timeline), ActivityEntry (field-change text templates). All type-check clean.</done>
</task>

<task type="auto">
  <name>Task 2: TaskDetailPage full replacement with two-panel layout</name>
  <files>
    apps/web/src/pages/TaskDetailPage.tsx
  </files>
  <read_first>
    apps/web/src/pages/TaskDetailPage.tsx,
    apps/web/src/components/tasks/RichTextEditor.tsx,
    apps/web/src/components/tasks/CommentThread.tsx,
    apps/web/src/components/tasks/AttachmentList.tsx,
    apps/web/src/components/tasks/ActivityLog.tsx,
    apps/web/src/hooks/useTasks.ts,
    apps/web/src/hooks/useProjectRole.ts,
    apps/web/src/lib/types.ts,
    apps/web/src/lib/api.ts,
    .planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-UI-SPEC.md
  </read_first>
  <action>
Completely replace `apps/web/src/pages/TaskDetailPage.tsx` with a new two-panel Jira-style layout per D-01 and D-02. The new page is a FULL REPLACEMENT — not a patch.

**Page structure** (matching UI-SPEC Layout Contract exactly):

```
Breadcrumb nav (Back to Backlog link)
Task Title (inline editable, text-xl semibold)
──── Separator ────
flex gap-8:
  LEFT PANEL (flex-1, flex flex-col gap-6):
    1. Description (RichTextEditor)
    2. Acceptance Criteria (checklist + "X/Y done" badge)
    3. Comments (CommentThread)
    4. Attachments (AttachmentList)
    5. Activity Log (ActivityLog)
  RIGHT SIDEBAR (w-60 shrink-0, sticky top-8):
    - Status select
    - Assignee select
    - Sprint select
    - Story Points input
    ── Separator ──
    - Sub-tasks mini-list
    ── Separator ──
    - Created by + date
    - Updated date
    ── Separator ──
    - Delete Task button (canManage only)
```

**Key implementation details:**

1. **Imports:** Import `RichTextEditor` from `@/components/tasks/RichTextEditor`, `CommentThread` from `@/components/tasks/CommentThread`, `AttachmentList` from `@/components/tasks/AttachmentList`, `ActivityLog` from `@/components/tasks/ActivityLog`. Import `useTask`, `useUpdateTask`, `useDeleteTask` from `@/hooks/useTasks`. Import `useProjectRole` from `@/hooks/useProjectRole`. Import `useMembers` from `@/hooks/useMembers` (for activity log member name resolution). Import `useSprints` from `@/hooks/useSprints` (for activity log sprint name resolution).

2. **Current user ID:** Get from `keycloak.tokenParsed?.sub` mapped to DB user ID. The existing codebase pattern for this should be read from `TaskDetailPage.tsx`. Check if there is a `useCurrentUser` hook or if the user ID comes from another source. The comment/attachment components need `currentUserId` to determine delete permissions.

3. **Title inline editing:** Reuse the existing pattern from the current TaskDetailPage — `editingTitle` state, `titleInputRef`, click-to-edit span, blur/Enter to save via `useUpdateTask`.

4. **Description section:**
```tsx
<section>
  <h2 className="text-[13px] font-semibold text-muted-foreground mb-2">Description</h2>
  <RichTextEditor
    initialContent={task.description ?? ''}
    onSave={(html) => updateTask.mutate({ taskId: task.id, data: { description: html } })}
    editable={canEdit}
  />
  {updateTask.isPending && (
    <div className="flex items-center gap-1 mt-1">
      <Loader2 className="size-3 animate-spin" />
      <span className="text-xs text-muted-foreground">Saving...</span>
    </div>
  )}
</section>
```

5. **Acceptance Criteria section** — Reuse `parseAcceptanceCriteria()` and `serializeAcceptanceCriteria()` helpers from the current page (copy them into the new file or extract to a shared util).
- Section header: "Acceptance Criteria" + `<Badge variant="secondary" className="text-xs">{checked}/{total} done</Badge>` (per D-12).
- Each item: `<Checkbox>` + inline-editable text span. Click text to switch to `<Input>`, blur/Enter commits, Escape cancels (per D-11, same pattern as title inline edit).
- Delete button: ghost X icon, `opacity-0 group-hover:opacity-100` (per UI-SPEC).
- "Add criteria" button: `<Button variant="ghost" size="sm" className="gap-1"><Plus className="size-3.5" /> Add criteria</Button>`. When clicked, shows inline Input, Enter to add, Escape to cancel.
- Empty state: "No acceptance criteria. Add the first one." (text-sm text-muted-foreground).

6. **Right sidebar** — `<div className="w-60 shrink-0"><div className="sticky top-8 flex flex-col gap-4">`:
- Status: `<Select>` with all TaskStatus values. onChange triggers `updateTask.mutate`.
- Assignee: `<Select>` with project members. Use `useMembers(projectId)`. onChange triggers `updateTask.mutate`.
- Sprint: `<Select>` with project sprints. Use `useSprints(projectId)`. onChange triggers `updateTask.mutate`.
- Story Points: `<Input type="number">` with blur-to-save.
- Sub-tasks: mini-list showing existing sub-tasks with status badges. Reuse existing sub-task create/update/delete patterns from current page.
- Meta info: Created by (username), Created date, Updated date — all with `text-xs text-muted-foreground`.
- Delete Task: `<Button variant="destructive" size="sm">Delete Task</Button>` visible only when `canManage`. Uses AlertDialog: Title "Delete Task", Body "This will permanently delete this task and all its sub-tasks. This action cannot be undone.", Confirm "Delete Task" (destructive), Cancel "Cancel". On confirm, delete task and navigate to backlog.

7. **Sidebar field labels:** Use `<span className="text-[11px] font-semibold uppercase tracking-wider text-muted-foreground">` for field labels (Status, Assignee, Sprint, Story Points) per UI-SPEC meta label typography.

8. **Loading skeleton:** Render the two-panel skeleton from UI-SPEC while task is loading.

9. **Error state:** If task not found or load error, show centered "This task doesn't exist or has been deleted." + link "Go to Backlog".

10. **Breadcrumb:** Simple `<Button variant="ghost" size="sm" className="gap-1" onClick={() => navigate(-1)}><ArrowLeft className="size-3.5" /> Back to Backlog</Button>` or equivalent navigation.
  </action>
  <verify>
    <automated>cd /Users/admin/Desktop/Projects/PM && pnpm --filter @pm/web exec tsc --noEmit 2>&1 | tail -10 && pnpm --filter @pm/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/pages/TaskDetailPage.tsx contains `RichTextEditor`
    - apps/web/src/pages/TaskDetailPage.tsx contains `CommentThread`
    - apps/web/src/pages/TaskDetailPage.tsx contains `AttachmentList`
    - apps/web/src/pages/TaskDetailPage.tsx contains `ActivityLog`
    - apps/web/src/pages/TaskDetailPage.tsx contains `sticky top-8` (sidebar pinned)
    - apps/web/src/pages/TaskDetailPage.tsx contains `w-60` (sidebar width)
    - apps/web/src/pages/TaskDetailPage.tsx contains `flex-1` (left panel)
    - apps/web/src/pages/TaskDetailPage.tsx contains `gap-8` (two-panel gap)
    - apps/web/src/pages/TaskDetailPage.tsx contains `parseAcceptanceCriteria` (AC helpers)
    - apps/web/src/pages/TaskDetailPage.tsx contains `done</Badge>` or `/Y done` (AC completion badge)
    - apps/web/src/pages/TaskDetailPage.tsx contains `AlertDialog` (delete task confirmation)
    - apps/web/src/pages/TaskDetailPage.tsx contains `Skeleton` (loading state)
    - `pnpm --filter @pm/web exec tsc --noEmit` exits 0
    - `pnpm --filter @pm/web build` exits 0
  </acceptance_criteria>
  <done>TaskDetailPage fully replaced with two-panel Jira-style layout. Left panel: Tiptap description editor, AC checklist with completion badge and inline editing, threaded comments, file attachments, activity timeline. Right sidebar: sticky metadata fields (status, assignee, sprint, points), sub-tasks, delete button. Loading skeleton and error states. All type-checks and builds pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete ticket page end-to-end</name>
  <what-built>Complete Jira-style ticket page with two-panel layout, Tiptap rich text description editor, threaded comments (create, reply, delete), file attachments (upload, download, delete with 10 MB limit), acceptance criteria with inline editing and completion badge, activity log showing field-level change history, and sticky right sidebar with metadata fields.</what-built>
  <how-to-verify>
    1. Start the API and web dev servers
    2. Navigate to any project's backlog and click on a task to open the detail page
    3. Verify two-panel layout: left content area + narrow right sidebar
    4. Verify sidebar is sticky (scroll the left panel — sidebar stays fixed)
    5. Click the description area — Tiptap editor should be active with toolbar (Bold, Italic, Bullet List, Numbered List, Code Block)
    6. Type some formatted text (bold, a list), click outside the editor — verify it auto-saves
    7. Add an acceptance criterion — verify inline input appears, Enter to add
    8. Check an AC item — verify the completion badge updates (e.g. "1/3 done")
    9. Click an AC item's text — verify inline edit mode, change text, blur to save
    10. Post a comment — verify it appears in the thread
    11. Reply to the comment — verify the reply appears indented below
    12. Delete a comment — verify AlertDialog confirmation, then removal
    13. Upload a file attachment (under 10 MB) — verify it appears in the attachment list
    14. Try uploading a file over 10 MB — verify error toast "File is too large. Maximum size is 10 MB."
    15. Click download on an attachment — verify file downloads
    16. Delete an attachment — verify AlertDialog confirmation, then removal
    17. Change the task status in the sidebar — verify the activity log shows a new entry (e.g. "moved to In Progress")
    18. Verify the activity log shows actor name and relative timestamp for each change
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
  <files>apps/web/src/pages/TaskDetailPage.tsx</files>
  <action>Human verification of the complete Jira-style ticket page. Run the dev servers and test all interactive flows.</action>
  <verify><automated>cd /Users/admin/Desktop/Projects/PM && pnpm --filter @pm/web build 2>&1 | tail -3</automated></verify>
  <done>All 18 verification steps pass. User approves the ticket page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser rendering | Tiptap HTML from task.description rendered in browser |
| client-side file validation | 10 MB size check before upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13 | Tampering | Tiptap HTML rendering | accept | Internal tool with authenticated users only. HTML stored as-is per D-05. React does not use dangerouslySetInnerHTML for comments (plain text). Tiptap renders its own content safely. DOMPurify deferred per CONTEXT.md. |
| T-08-14 | Elevation of privilege | Client-side delete visibility | mitigate | Delete buttons hidden unless `comment.authorId === currentUserId OR canManage`. Server-side enforcement is the real gate (Plan 02). |
| T-08-15 | Denial of Service | Client-side file size | mitigate | Client checks `file.size > 10_485_760` before upload. Server also enforces via Multer limits (Plan 02). Defense in depth. |
</threat_model>

<verification>
- `pnpm --filter @pm/web exec tsc --noEmit` exits 0
- `pnpm --filter @pm/web build` exits 0
- TaskDetailPage.tsx renders two-panel layout with all 5 left-panel sections
- All 6 new components exist and are used by TaskDetailPage
- Human verification of full interactive flow
</verification>

<success_criteria>
- Two-panel layout with sticky sidebar
- Tiptap editor with toolbar and blur auto-save
- AC section with inline editing, completion badge, add/delete
- Threaded comments with reply and delete
- File attachments with upload, download, delete, 10 MB limit
- Activity log with field-change history and actor names
- Loading skeleton and error states
- All interactions work end-to-end (frontend to backend)
</success_criteria>

<output>
After completion, create `.planning/phases/08-jira-style-backlog-ticket-page-with-rich-editor-ac-tracking-/08-04-SUMMARY.md`
</output>
